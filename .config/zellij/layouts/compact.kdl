// Compact layout - minimal UI similar to tmux setup
layout {
    // Status bar at bottom (like tmux)
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
        children
    }

    // Default single pane
    tab name="main" {
        pane
    }
}
